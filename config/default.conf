
server {
    autoindex on;
    listen 8080;
    root /www;
    autoindex on;
    client_max_body_size 1m;
    location /uploads/ {
        root /www;
        autoindex on;
    }
}
server {
    listen 8080;
    autoindex on;
    root /www;
}